// u-becerra
$('#audio-ub').on('timeupdate', function() {
    $('#seekbar-ub').attr("value", this.currentTime / this.duration);
});

// u-guti
$('#audio-ug').on('timeupdate', function() {
    $('#seekbar-ug').attr("value", this.currentTime / this.duration);
});

// u-gana
$('#audio-ugn').on('timeupdate', function() {
    $('#seekbar-ugn').attr("value", this.currentTime / this.duration);
});

// u-yanez
$('#audio-uy').on('timeupdate', function() {
    $('#seekbar-uy').attr("value", this.currentTime / this.duration);
});

// u-quiroz
$('#audio-uq').on('timeupdate', function() {
    $('#seekbar-uq').attr("value", this.currentTime / this.duration);
});

// u-orellana
$('#audio-uo').on('timeupdate', function() {
    $('#seekbar-uo').attr("value", this.currentTime / this.duration);
});

// u-zanartu
$('#audio-uz').on('timeupdate', function() {
    $('#seekbar-uz').attr("value", this.currentTime / this.duration);
});

// u-manda
$('#audio-um').on('timeupdate', function() {
    $('#seekbar-um').attr("value", this.currentTime / this.duration);
});

// u-ibarra
$('#audio-ui').on('timeupdate', function() {
    $('#seekbar-ui').attr("value", this.currentTime / this.duration);
});

// u-abarca
$('#audio-ua').on('timeupdate', function() {
    $('#seekbar-ua').attr("value", this.currentTime / this.duration);
});


//e-roa
$('#audio-er').on('timeupdate', function() {
    $('#seekbar-er').attr("value", this.currentTime / this.duration);
});
//e-ortega
$('#audio-eo').on('timeupdate', function() {
    $('#seekbar-eo').attr("value", this.currentTime / this.duration);
});
//e-ulloa
$('#audio-eu').on('timeupdate', function() {
    $('#seekbar-eu').attr("value", this.currentTime / this.duration);
});
//e-cortez
$('#audio-ec').on('timeupdate', function() {
    $('#seekbar-ec').attr("value", this.currentTime / this.duration);
});
//e-rivera
$('#audio-erv').on('timeupdate', function() {
    $('#seekbar-erv').attr("value", this.currentTime / this.duration);
});
//e-perez
$('#audio-ep').on('timeupdate', function() {
    $('#seekbar-ep').attr("value", this.currentTime / this.duration);
});
//e-faundez
$('#audio-ef').on('timeupdate', function() {
    $('#seekbar-ef').attr("value", this.currentTime / this.duration);
});
//e-mazuela
$('#audio-em').on('timeupdate', function() {
    $('#seekbar-em').attr("value", this.currentTime / this.duration);
});
//e-torres
$('#audio-et').on('timeupdate', function() {
    $('#seekbar-et').attr("value", this.currentTime / this.duration);
});
//e-fdz
$('#audio-efz').on('timeupdate', function() {
    $('#seekbar-efz').attr("value", this.currentTime / this.duration);
});